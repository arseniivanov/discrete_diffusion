defaults:
  - base_config  # Inherit all the defaults from your main config
  - override hydra/sweeper: basic # Specify we are using the basic sweeper

hydra:
  sweeper:
    params:
      noise.type: log_linear, masking
      noise.schedule: cosine, linear
